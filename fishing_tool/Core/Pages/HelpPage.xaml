<Page x:Class="fishing_tool.Core.Pages.HelpPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:fishing_tool.Core.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding HelpViewModel, Source={StaticResource VmLocator}}"
      Title="HelpPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 20px ">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" 
                    Width="200px" 
                        Foreground="Black"
                        BorderThickness="1" 
                        BorderBrush="Black"
                        FontWeight="Bold"
                        Height="35" 
                        Background="#FF9D3FA1"
                        FontSize="{StaticResource Regular }"
                        Padding="0" 
                        Margin="0 0 10px 0"
                        Content="Сгенерировать пример" Command="{Binding generateExample}" >
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="7"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
        <Image  Grid.Row="1" Source="pack://application:,,,/Assets/Images/example.png" />
    </Grid>
</Page>
