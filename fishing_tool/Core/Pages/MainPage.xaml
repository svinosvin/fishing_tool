<Page x:Class="fishing_tool.Core.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:fishing_tool.Core.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding FirstViewModel, Source={StaticResource VmLocator}}"
      Title="MainPage">
    <Grid Margin="20px">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel  Grid.Row="0">
            <Button  Width="300px" 
                        Foreground="Black"
                        BorderThickness="1" 
                        BorderBrush="Black"
                        FontWeight="Bold"
                        Height="35" 
                        Background="#FF9D3FA1"
                        FontSize="{StaticResource Regular }"
                        Padding="0"
                        Content="Выбрать соревнования" Command="{Binding GetCompetition}"/>
                <ComboBox  
                          SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                          FontSize="{StaticResource Regular}"
                          Grid.Row="0" Margin="10 15 0 10"  
                          Width="130">
                <TextBlock>Командные зачеты</TextBlock>
                <TextBlock>Личный зачет</TextBlock>
            </ComboBox>

        </WrapPanel>
        <DataGrid  Grid.Row="1" ItemsSource="{Binding Teams}" AutoGenerateColumns="True" Visibility="{Binding Path=DataMode1, Converter={StaticResource BoolToVis}}"/>
        <DataGrid  Grid.Row="1" ItemsSource="{Binding Fishers}" AutoGenerateColumns="True" Visibility="{Binding Path=DataMode2, Converter={StaticResource BoolToVis}}"/>

    </Grid>
    
</Page>
